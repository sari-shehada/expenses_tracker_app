import 'package:flutter/animation.dart';

abstract final class AppMotion {
  static const colorTransitionDuration = Duration(milliseconds: 180);
  static const colorTransitionCurve = Curves.easeOut;
}
