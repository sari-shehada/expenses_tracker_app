import 'package:flutter/widgets.dart';

class AddVerticalSpacing extends SizedBox {
  const AddVerticalSpacing(double height, {super.key}) : super(height: height);
}

class AddHorizontalSpacing extends SizedBox {
  const AddHorizontalSpacing(double width, {super.key}) : super(width: width);
}
